next:
- build: proxy
	build_in: lib
	config:
		clear_build_path: false
		proxy: "../config/libtruckeefs.yaml"
    next:
    - build: proxy
    	build_in: ../exe
    	config:
    		clear_build_path: false
    		proxy: "../../config/truckeefs.yaml"
